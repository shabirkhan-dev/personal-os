# Postgres service — included from root docker-compose.yml
services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-personal-os}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-personal-os}
      POSTGRES_DB: ${POSTGRES_DB:-personal-os}
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-personal-os} -d ${POSTGRES_DB:-personal-os}",
        ]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  postgres_data:
